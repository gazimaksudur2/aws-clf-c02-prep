; Use PowerShell as the script shell on Windows so npm scripts work
; even when the project path contains characters like '&' or spaces.
; cmd.exe interprets unquoted '&' in PATH as a command separator,
; which breaks `npm run` from any path containing '&'.
script-shell=powershell.exe
